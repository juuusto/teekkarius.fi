import styled from 'styled-components';

const Footer = () => {
  return <StyledFooter> this the footer bois </StyledFooter>;
};

const StyledFooter = styled.footer`
  width: 100%;
  min-height: 4em;
  background: #cacaca;
  text-align: center;
  margin-top: auto;
`;
export default Footer;
